# P10 Verification Record

## Objective and scope

P10 defines and freezes a documentation-and-test-only research contract for
the 2Y/5Y swap-spread equations, economic examples, signal/state transitions,
integer-DV01 execution examples, contract P&L, timing, and input lineage. It
creates no production strategy interface, changes no production strategy
behavior, and stops at MG2 for manual approval. The comparison base is
`49fad29a4b70824654b29671d62bb5c1d09d8467`.

## Code and design commits

The P10 branch history through the start of Task 5 is:

- `c04efc8` — `docs: design P10 strategy equations`
- `ba44d8c` — `docs: plan P10 strategy equations`
- `bfff861` — `docs: make P10 fixture RED intentional`
- `1b5b44f` — `docs: establish P10 source contract`
- `75d333a` — `docs: clarify P10 classification lineage`
- `6032330` — `test: add P10 economic golden equations`
- `682f6d3` — original `test: freeze P10 execution equations`
- `7978c4d` — `docs: require assertion-based P10 RED cycles`
- `6d2c5ed` — normal revert of the original execution-equation implementation
- `fc07f10` — corrected chronological `test: freeze P10 execution equations`
- `29b3b19` — `docs: complete P10 equation contract`
- `b5301f4` — `docs: clarify P10 contract input validation`
- `d14af7d` — `docs: restore P10 P&L input gate`
- `8cde1e3` — `test: isolate P10 decimal context` (quality correction)
- `831fe38` — `test: freeze P10 causal clocks` (causality correction)
- `4e7a8d3` — `test: preserve saved P10 movement` (causality re-review correction)
- `21f6e11` — `test: freeze P10 market units` (market-units correction)
- `ff574eb` — `docs: record P10 verification evidence`
- `38805aa` — `test: validate P10 causal identities` (senior-review correction)

## Files changed

Before this record, `git diff --name-status
49fad29a4b70824654b29671d62bb5c1d09d8467...d14af7d` reported exactly five
added files:

- `docs/research/strategy-equations.md`
- `docs/superpowers/plans/2026-07-31-p10-strategy-equations.md`
- `docs/superpowers/specs/2026-07-29-p10-strategy-equations-design.md`
- `tests/fixtures/strategy_equation_examples.json`
- `tests/test_strategy_equation_examples.py`

Task 5 adds only this verification record unless an independent reviewer finds
a concrete defect. Any such correction must be committed separately before
the evidence commit.

Independent review produced separately committed correction waves in the same
three allowed implementation paths: Decimal-context isolation in the test
module, dated causality clocks in the research/fixture/tests, exact market-unit
and full-identity coverage in the research/fixture/tests, and the final senior-
review decision-clock/roll-identity guards in the research/tests. Commit
`ff574eb` added `docs/verification/P10.md` as the sixth P10-owned path; this
refresh changes no additional path.

## Unrelated changes preserved

The Task 5 starting status was clean on `codex/p10-equations`. The complete
pre-evidence P10 diff contained only the five P10-owned paths listed above.
No unrelated file was edited, staged, deleted, restored, or overwritten.

## Tests written before implementation

The Task 1 fixture-absence RED exited 1 with the required `missing fixture:`
assertion before the fixture existed. Tasks 2–4 used assertion-only missing-
callable RED cycles before each test-only helper implementation. The corrected
Task 3 history normally reverted the first implementation and then recorded
chronological RED/GREEN cycles with assertion failures and zero missing-name,
import, or type errors. The task reports preserve the exact cycle commands and
outputs. No production helper was introduced; all equation oracles remain in
`tests/test_strategy_equation_examples.py`.

Task 5 also followed assertion-first TDD for every behavioral correction:

- quality isolation: RED 24 pass/1 fail, then GREEN 25/25;
- causality wave: RED 24 pass/4 fail with zero errors, then GREEN 28/28;
- saved-movement replay: targeted RED 0/1, then GREEN 1/1; and
- market units/full identities: RED 25 pass/6 fail with zero errors, then
  GREEN 31/31; and
- senior-review clock/identity guards: RED 31 pass/5 fail with zero errors,
  then GREEN 36/36; and
- final whole-branch roll-root/month guards: RED 36 pass/2 fail with zero
  errors, then GREEN 38/38.

## Command evidence

The pre-review baseline was 24/24 focused and 78/78 full. The first complete
review matrix was 31/31 focused and 85/85 full. After the senior-review and
final whole-branch corrections, the complete fresh final matrix produced:

| Command | Exit | Exact result | Controller elapsed |
|---|---:|---|---:|
| focused guarded unittest discovery | 0 | 38 tests in 0.023 s; `OK` | 0.7 s tool wall |
| full guarded unittest discovery | 0 | 92 tests in 0.375 s; `OK` | 1.7 s tool wall |
| `python -m compileall -q` over the required targets | 0 | no diagnostics | 109 ms |
| `raw_price_data.py --self-check` | 0 | `[OK] self-check passed` | 598 ms |
| `signal_data.py --self-check` | 0 | `[OK] self-check passed` | 579 ms |
| `risk_data.py --self-check` | 0 | `[OK] self-check passed` | 620 ms |
| `backtest.py --self-check` | 0 | `[OK] self-check passed` | 627 ms |
| `python -m pip check` | 0 | `No broken requirements found.` | 450 ms |
| `git diff --check` | 0 | no whitespace errors | 46 ms |
| `git status --short` | 0 | one expected evidence-refresh path before staging | 43 ms |
| base-to-HEAD allowed-scope check | 0 | 6 P10 paths; `UNEXPECTED_PATHS=0` | 61 ms |

Every guarded command found `agents/agent_0/orders` absent. The full suite's
Agent 0 console messages came from existing guarded fake objects and local
fixtures; no external connection or broker mutation occurred.

The final six-file fail-closed hygiene scan exited 0. It scanned all six files
and reported zero missing files, scan exceptions, invalid-UTF-8 files,
NUL-byte files, merge markers, unbalanced Markdown-fence files, unfinished-
work markers, AWS access keys, private-key markers, credential assignments,
unexpected paper-account patterns, and total hygiene errors. It emitted counts
only and never printed matched secret text.

## Independent golden recalculations

The financial reviewer parsed the fixture directly with an independent
standard-library Decimal audit, imported no test module/helper, wrote no file,
and reported `MISMATCH_COUNT=0`. Its 50-digit calculations reproduced:

| Example | Gross bps | Cost USD / target DV01 | Directional net bps | 252-prior mean / sample SD | z | Eligible |
|---|---:|---:|---:|---:|---:|---|
| traditional 2Y | 25 | 1,000 / 1,000 = 1 | 24 | 15 / 5 | 2 | yes |
| traditional 5Y | 15 | 3,000 / 1,000 = 3 | 12 | 0 / 5 | 3 | yes |
| reverse 2Y | -45 | 2,000 / 1,000 = 2 | 43 | -25 / 10 | -2 | yes |
| reverse 5Y | -18 | 1,500 / 1,000 = 1.5 | 16.5 | -5 / 5 | -2.6 | yes |

Each sample standard deviation used 252 prior observations and denominator
`n-1 = 251`: sums of squared deviations 6,275 produce variance 25/SD 5,
while 25,100 produces variance 100/SD 10. The audit also reproduced the 60,
40, and 39-observation funding availability cases, the 20-step constant-
expectation average, and exclusion of an older 999-bp observation after the
trailing window reached 60 observations.

Movement results were exactly `4.99 -> 0`, `5.00 -> +1`, `5.01 -> +1`,
`-4.99 -> 0`, `-5.00 -> -1`, and `-5.01 -> -1`. All eight state cases,
inclusive entry/exit thresholds, strict-positive eligibility, ordered
reversal, and flatten precedence matched. Same-contract P&L reproduced USD 50
traditional and USD 20 reverse. Roll arithmetic reproduced `20 + 10 - 3 - 4
= 23`, turnover 4, while rejecting the USD -420 cross-contract artifact.
Reversal arithmetic reproduced USD 600 cost and turnover 5.

All seven integer-DV01 expected tuples reproduced at 50-digit precision. The
signed residual boundaries were exactly 0.0499 allowed, 0.05 allowed, and
0.0501 blocked; 2.5 rounded half-away to 3; the lower-gross candidate won the
tie; and a rounded zero swap leg blocked. The displayed repeating `100/300`
residual matched the fixture under the declared 50-digit context.

## Data read or changed

P10 reads repository documentation, the approved design and plan, the JSON
fixture, tests, prior P01/P02 evidence, and version-control/test output. Earlier
P10 research used official primary documentation to establish conventions and
source boundaries. During the final market-unit correction, the controller
re-verified only the already-approved official CME Eris and Treasury contract-
specification documentation, including the current Treasury specification
lesson, delivery guide, and the published ZT tick-change notice. These were
official-documentation reads, not market-data reads or API calls. No public or
private market-data request was made. All numerical example inputs are
explicitly synthetic. No broker state, account value, order file,
Cloudflare/R2 object, Quantt response, or live market observation was read or
changed.

## External systems contacted

- IBKR connections: 0
- Broker-order submissions: 0
- Broker-order modifications: 0
- Broker-order cancellations: 0
- Cloudflare/R2 calls: 0
- Quantt calls: 0
- Public market-data requests: 0
- Broker-order submission count during P10 development: 0
- Official-documentation reads: approved CME convention/specification pages
  and PDFs only; no quote, settlement, historical-price, or account data

The full-suite characterization output is produced by guarded fake objects and
local test fixtures; it is not evidence of an external broker connection.

## Requirements review

**Verdict: PASS.** The independent reviewer found no actionable requirement
gap, scope violation, missing required golden case, proxy mislabelling,
paper-only violation, or contradiction. At review snapshot `d14af7d`, evidence
included:

- the documentation/test-only boundary in the approved design lines 21–29 and
  implementation plan lines 15–18;
- economic/executable separation and the synthetic 2Y/5Y boundary in the
  research contract lines 9–25, four directional fixture examples at lines
  47–155, and independent Decimal checks in the test module lines 271–276;
- movement, state, P&L, reversal, roll, and DV01 fixtures at lines 157–188 and
  their tests at lines 336–553;
- the exclusive input classifications and proxy boundary in the research
  contract lines 47–68 and 457–477, with fail-closed lineage tests at test
  lines 667–732; and
- the paper-only and manual-MG2 stop in the master plan lines 30–32, approved
  design lines 314–327, this record, and the unchanged MG2 ledger row.

The reviewer reran the focused command: exit 0, 24 tests in 0.006 seconds,
`OK`. `git diff --check` exited 0; the complete 2,909-line base-to-HEAD diff
contained only the five expected added files; `UNEXPECTED_PATHS=0`; status was
clean; MG2 remained `Not started`; and `agents/agent_0/orders` was absent.

## Quality review

**Initial verdict: FAIL.** One P2 test-isolation finding was actionable:
`tests/test_strategy_equation_examples.py` changed the process-wide Decimal
precision from 28 to 50 at module import. The reviewer reproduced
`PRECISION_BEFORE=28` and `PRECISION_AFTER=50`; all other quality dimensions
passed, including Decimal string construction, exact comparisons,
fixture/oracle separation, deterministic local inputs, fail-closed invalid
inputs, test-local helper scope, UTF-8/Markdown integrity, and unrelated-work
preservation. Its focused rerun exited 0 with 24 tests in 0.005 seconds and
`OK` before the correction.

The finding was independently reproduced, then corrected under TDD. A new
module-loading isolation test was written first. RED exited 1 with exactly one
assertion failure, `50 != 28`; the other 24 tests passed. The minimal change
moved 50-digit precision into `setUpModule`, captured the prior Decimal
context, and restored it with `setcontext` in `tearDownModule`. GREEN exited 0,
ran 25 tests in 0.015 seconds, reported `OK`, and had controller elapsed time
166 ms. The direct reproduction then reported `PRECISION_BEFORE=28`,
`PRECISION_AFTER=28`, exit 0. The isolated correction commit is `8cde1e3`
(`test: isolate P10 decimal context`). The scoped read-only re-review is
recorded below.

**Scoped re-review verdict: PASS.** At correction snapshot `8cde1e3`, the
reviewer confirmed the regression test at test lines 786–792 directly loads
the module under precision 28 and protects the caller context; `setUpModule`
at lines 23–26 copies the complete prior
context before selecting precision 50; and `tearDownModule` at lines 29–31
restores the complete context with `setcontext`. Its fresh direct probe reported
28 before and 28 after, exit 0. Its fresh focused suite exited 0, ran 25 tests
in 0.022 seconds, and reported `OK`. The correction range changed only the test
module, `git diff --check` exited 0, and no new breakage, expected-value or
tolerance weakening, scope expansion, production change, MG2 change, external
contact, or order directory was found.

## Financial-equations review

**Verdict: PASS.** Every fixture expected value was independently reproducible;
no mismatch or actionable finding remained. At review snapshot `8cde1e3`,
normative evidence was cited at research-contract lines 92–134, 147–217,
177–206, 231–254, 271–362, and 375–454; fixture evidence at lines 4–188. The
fresh focused suite exited 0,
ran 25 tests in 0.017 seconds, reported `OK`, and had controller elapsed time
161 ms. Scope remained five expected P10 paths with `UNEXPECTED_PATHS=0`;
`git diff --check` exited 0; MG2 remained `Not started`; the order directory
was absent; and every external-contact count for the review was zero.

## Causality review

**Initial verdict: FAIL.** The independent reviewer found three P1 timing gaps:

1. The approved Agent 2 movement clock was documented as undefined
   `Delta r` and tested only as a scalar, rather than
   `Delta SS(m,t) = SS(m,t) - SS(m,t-1)` over adjacent synchronized completed
   business dates with publication cutoffs.
2. Funding and z-score helpers did not select from availability-timestamped
   records at the saved decision. Funding silently collapsed duplicate dates,
   z-score accepted an undated 252-value list, and the old future perturbation
   sliced its added value away before calculation.
3. The roll fixture had no close/open transition timestamps, so it did not
   prove when the old quantity stopped earning P&L, when the new quantity
   started, or that the intervals had no overlap or gap.

All three were treated as actionable against the approved design and master
backtest contract. Assertion-first tests were added before helpers. RED exited
1 and ran 28 tests with exactly four assertion failures and zero errors: two
missing `select_causal_history` guards, one missing
`synchronized_spread_movement` guard, and one missing `causal_roll_pnl` guard;
the other 24 tests passed.

The minimal correction makes dated funding and z-score selection ignore
current/future/late-revision rows, reject eligible duplicates and gaps, and
enforce the saved publication cutoff. Movement now constructs adjacent
synchronized CMS/CMT swap spreads at both saved decisions and applies the
exact `Delta SS` formula. Roll timestamps now require
`old_end = close = roll = open = new_start`, count only completed as-of
same-contract intervals, and prove a future new-contract price cannot change
old P&L saved at the roll. The normative research contract and timestamped
roll fixture were updated consistently.

GREEN exited 0, ran 28 tests in 0.018 seconds, reported `OK`, and had
controller elapsed time 152 ms. The covering full suite exited 0, ran 82 tests
in 0.387 seconds, reported `OK`, and had controller elapsed time 3,152 ms.
Correction scope was exactly the research contract, fixture, and test module;
`git diff --check` exited 0; no order directory existed. The separate correction
commit is `831fe38` (`test: freeze P10 causal clocks`). A scoped read-only
causality re-review is recorded below.

**Scoped re-review round 1 verdict: FAIL.** Funding/z-score and roll findings
were fully addressed. The movement finding was substantially addressed but
one replay case remained: a later-published fifth record for a saved endpoint
date was selected before the saved cutoff was applied, making the exact-field
check return unavailable instead of ignoring the future revision.

Round 2 used assertion-first TDD. The new test appends a same-endpoint revision
published after the saved decision and requires the original 5-bp movement to
remain unchanged; it also proves an eligible same-date duplicate still blocks.
Targeted RED exited 1 with one assertion failure, `None` versus the saved
movement result, and zero errors. The one-line behavior change filters endpoint
records by their saved `decision_utc` before exact four-field validation.
Targeted GREEN exited 0, ran 1 test, reported `OK`, and had controller elapsed
time 131 ms. The covering focused suite exited 0, ran 28 tests in 0.019 seconds,
reported `OK`, and had controller elapsed time 144 ms. The full suite exited 0,
ran 82 tests in 0.309 seconds, reported `OK`, and had controller elapsed time
961 ms. The round changed only the test module; `git diff --check` exited 0;
the order directory remained absent. Separate commit: `4e7a8d3`
(`test: preserve saved P10 movement`). The second scoped re-review is recorded
below.

**Scoped re-review round 2 verdict: PASS.** The reviewer confirmed that each
endpoint snapshot now filters by `available_utc <= decision_utc` before exact
field validation, so a later same-endpoint revision is excluded while an
eligible duplicate remains fail-closed. At snapshot `21f6e11`, it cited the
helper at test lines 168–184 and both regression branches at lines 1093–1117.
Its fresh focused
suite exited 0, ran 28 tests in 0.017 seconds, and reported `OK`. The range
changed only the test module, `git diff --check` exited 0, MG2 remained `Not
started`, the order directory remained absent, and no new breakage, expected-
value/tolerance weakening, production change, external contact, or broker
action was found. All three original causality findings are addressed.

**Senior-review follow-up: actionable, corrected.** A later senior review found
that adjacent observation dates did not themselves prove an increasing saved
decision clock. Two assertion-first regressions used otherwise complete
snapshots with equal and reversed decisions. RED returned the normal 5-bp
movement in both cases, producing two assertion failures and zero errors. The
minimal guard now requires
`previous_decision_utc < current_decision_utc`; equal or reversed clocks return
unavailable. These cases pass in correction commit `38805aa` without changing
any expected economic value or tolerance.

## Market-units review

**Initial verdict: FAIL.** The independent reviewer found two P1 gaps while
confirming that direction, root mapping, point multipliers, CTD-DV01 authority,
ETU/EWV ratios, EAT-vs-ETU identity, signed P&L, roll arithmetic, and all seven
hedges were otherwise internally correct:

1. The source ledger and fixture froze point multipliers but not official
   minimum price increments, tick values, Treasury fractional-source-quote
   normalization, or tick-grid assertions.
2. The two examples named `same_contract` carried root-only symbols, so they
   did not prove identical full expiry identity at both price endpoints.

Both findings were corrected with assertion-first TDD using the already-
approved official CME source facts. RED exited 1 and ran 31 tests with six
assertion failures and zero errors: missing callable guards for the Treasury
quote normalizer and tick-value helper, plus four root-only identity failures.
No expected value was changed.

The normative and fixture contract now freezes:

- YIT: 0.0025 point × USD 1,000/point = USD 2.50/tick;
- YIW: 0.0100 point × USD 1,000/point = USD 10.00/tick;
- ZT futures: 1/8 of 1/32 = 0.00390625 point × USD 2,000/point =
  USD 7.8125/tick; and
- ZF futures: 1/4 of 1/32 = 0.0078125 point × USD 1,000/point =
  USD 7.8125/tick.

The exact Treasury normalization is
`P = whole + thirty_seconds/32 + eighths_of_32nd/256`. ZTH27 `(102,0,0)` and
`(101,31,4)` normalize to 102 and 101.984375; ZFH27 `(108,0,0)` and
`(108,0,4)` normalize to 108 and 108.015625. The P&L fixture now uses immutable
full IDs YITH27/ZTH27 and YIWH27/ZFH27 at both endpoints. Every price is tested
on its instrument's official tick grid.

GREEN exited 0, ran 31 tests in 0.020 seconds, reported `OK`, and had
controller elapsed time 157 ms. A separate fixture-direct Decimal audit
reported all four tick values, both Treasury quote conversions, unchanged USD
50/USD 20 P&Ls, `MISMATCH_COUNT=0`, and exit 0. The covering full suite exited
0, ran 85 tests in 0.342 seconds, reported `OK`, and had controller elapsed time
1,116 ms. Correction scope was exactly the research contract, fixture, and
test module; `git diff --check` exited 0; no order directory existed. The
separate commit is `21f6e11` (`test: freeze P10 market units`). A scoped
read-only re-review is recorded below.

**Scoped re-review verdict: PASS.** Both original P1 findings are addressed.
At snapshot `21f6e11`, the reviewer cited the source ledger at research lines
79–83, normalization and examples at lines 92–122, fixture conventions at
lines 4–32, full IDs and quote tuples at lines 210–276, and exact tests at test
lines 548–651. Its
independent no-helper arithmetic reproduced all four tick values, both
Treasury quote conversions, every non-roll and roll tick-grid integer, the
unchanged USD 50/USD 20 examples, and the USD 23 roll with
`MISMATCH_COUNT=0`. Its fresh focused suite exited 0, ran 31 tests in 0.020
seconds, reported `OK`, and had controller elapsed time 159 ms. The correction
range contained exactly the three approved P10 paths; diff check exited 0;
there was no tolerance weakening, production path, MG2 change, order
directory, external/broker action, or new actionable issue.

**Senior-review follow-up: actionable, corrected.** The senior review found
that the roll oracle enforced transition times but did not validate the old or
new contract identity. Three assertion-first regressions showed that root-only,
malformed, and identical old/new symbols all produced the normal USD 23 result
before correction. RED therefore had three more assertion failures and zero
errors. Commit `38805aa` now requires both symbols to match the declared full-
expiry grammar and requires the identities to differ; all three malformed
cases fail closed while the unchanged YITH27-to-YITM27 example still produces
USD 23.

## Findings and resolutions

Every concrete review finding is recorded with its disposition, correction
commit and covering rerun when applicable. No expected value or tolerance may
be weakened to make a finding disappear.

- Requirements review: no actionable finding; PASS without correction.
- Quality review P2: process-wide Decimal context mutation at module import.
  Resolved by regression-first module lifecycle isolation in commit `8cde1e3`;
  RED 24 pass/1 fail, GREEN 25/25 pass, direct before/after precision 28/28.
  Scoped re-review: PASS; original finding addressed and no new breakage.
- Financial-equations review: no mismatch or actionable finding;
  fixture-direct independent recalculation `MISMATCH_COUNT=0`; PASS.
- Causality review P1 findings: adjacent synchronized `Delta SS`, timestamped
  funding/z-score selection, and gap-free roll transition clocks were missing.
  Corrected assertion-first in `831fe38`; RED 24 pass/4 fail with zero errors,
  GREEN 28/28, covering full suite 82/82. Round-1 scoped re-review addressed
  funding/z-score and roll but kept late same-endpoint movement replay open.
  Round 2 in `4e7a8d3`: targeted assertion RED 0/1, GREEN 1/1, focused 28/28,
  full 82/82; second scoped re-review PASS with all original findings
  addressed and no new breakage.
- Market-units review P1 findings: official quote/tick conversion and full
  same-contract expiry identity were missing. Corrected assertion-first in
  `21f6e11`; RED 25 pass/6 fail with zero errors, GREEN 31/31, independent
  `MISMATCH_COUNT=0`, full 85/85. Scoped re-review PASS with both original
  findings addressed and no new actionable issue.
- Senior review Important findings: synchronized movement accepted equal or
  reversed saved decision clocks, and roll P&L accepted root-only, malformed,
  or identical old/new contract identities. Five assertion-first regressions
  produced RED 31 pass/5 fail with zero errors. Commit `38805aa` adds only the
  two fail-closed guards and matching normative text; GREEN was 36/36 and the
  covering full suite was 90/90. Economics and tolerances are unchanged.
- Senior review Minor finding: historical line references became stale after
  later test insertions. The references now name the exact commit snapshot
  they describe instead of presenting historical line numbers as current.
- Final whole-branch review Important finding: the full-ID roll gate accepted
  cross-product roots and non-quarterly month codes. Two assertion-first
  regressions produced RED 36 pass/2 fail with zero errors. The final guard
  requires the same root and the listed `H/M/U/Z` quarterly cycle; GREEN was
  38/38 and the covering full suite was 92/92. Existing economics and
  tolerances remain unchanged.

## Known limitations

The production business-day/holiday calendar, maturity-matched CMS history,
exact floating-reference mapping, maturity/collateral-consistent repo input,
forward funding curve, intraday trigger, and 10Y/30Y executable baskets remain
unavailable pending later validation. `EFFR-SOFR` remains a named proxy, never
an exact `L-repo` replacement. The 2Y/5Y market values and execution costs in
the examples are synthetic assumptions; P10 does not claim a complete
production strategy or validated live execution inputs.

## MG2 manual gate request

All five independent reviews, the senior-review corrections, and the complete
final matrix are now recorded.
This evidence record requests manual MG2 review; it does not approve MG2,
update the gate ledger, authorize a merge or push, or authorize P11. MG2
remains `Not started` until the user explicitly approves the evidence.

Manual review should inspect the normative contract at
`docs/research/strategy-equations.md`, the exact synthetic fixture at
`tests/fixtures/strategy_equation_examples.json`, and its independent Decimal
oracle at `tests/test_strategy_equation_examples.py`, then:

1. recalculate all four economic examples and their 252-prior sample moments;
2. confirm the 60/40 funding estimator, 20-day horizon, dated publication
   cutoffs, adjacent `Delta SS`, thresholds, state precedence, and roll clock;
3. confirm Eris/Treasury directions, full contract identities, quote/tick
   conversions, point multipliers, signed P&L, and current CTD-DV01 authority;
4. confirm proxy/unavailable boundaries and the 2Y/5Y-only executable scope;
5. review focused 38/38, full 92/92, independent `MISMATCH_COUNT=0`, hygiene
   zeros, external-contact zeros, and the known unavailable inputs; and
6. explicitly approve or reject MG2 without modifying this record implicitly.

## Paper-only assertion

P10 adds no production account, port, endpoint, toggle, capital path, broker
adapter, or order-routing behavior. It performs no broker connection or broker
mutation. `agents/agent_0/orders` must remain absent at every verification
checkpoint, and all work stops for manual MG2 approval.
