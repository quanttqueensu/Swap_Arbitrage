# P32 signal-generation verification

## Scope and provenance

P31 passed MG5 equation comparison at commit `9d92519`. P32 began from plan
commit `1d2e3f2`; `1d2e3f2..6721e49` is the initial Task 3 implementation
range, not the final reviewed range. The senior whole-branch review examined
pre-fix head `376b067`. Subsequent review work includes runtime validation fix
`5ca24e9`, transition simplification `b50b441`, range evidence correction
`6e43d2c`, documentation clarification `b34d1c7`, specialist evidence
`376b067`, and nullable-z runtime fix `b02f315`.
The frozen fixture is
`docs/tests/fixtures/strategy_equation_examples.json`, schema version
`p10.strategy-equations.v1`, SHA-256
`3fb9da5fc9ad255587ce93ea9770552f42566d56070f68ad1661709c030fbd76`.

P32 has no fixed runtime strategy version: `generate_signal_decision` preserves
the caller-supplied nonblank strategy and configuration version strings. This
increment adds only the direct standard-library `strategy.signal_generation`
functions and public exports. It does not modify the fixture,
`signal_pipeline.py`, or a data source, clock, calendar, order, broker, or
network path.

## Public contracts and exact policies

| API | Exact contract |
| --- | --- |
| `causal_zscore(current: SpreadObservation, prior: object) -> Decimal | None` | Exact 252-row, same-maturity, strictly increasing UTC history before current; sample standard deviation from prior gross excess only; local precision 50; zero variance and malformed input return `None`. |
| `signal_transition(prior_state: PositionState, z_score: Decimal | None, traditional_net_bps: Decimal, reverse_net_bps: Decimal, data_ready: bool, risk_flatten: bool) -> tuple[PositionState, tuple[str, ...]] | None` | Exact states, finite Decimals, and bools only; risk precedes unavailable data, then inclusive entry at `2.0`/`-2.0`, strict-positive directional nets, and `abs(z) <= 0.5` exit hysteresis. |
| `generate_signal_decision(decision_id: str, observation: SpreadObservation, prior: object, prior_state: PositionState, risk_flatten: bool, strategy_version: str, configuration_version: str) -> SignalDecision | None` | Recomputes the causal z-score; ready requires exact true quality/freshness, count `252`, and, when a declared z-score is non-null, equality with the recomputed value. |
| `rank_opportunities(observations: object) -> tuple[str, ...] | None` | Non-string `Sequence` of exact synchronized unique-maturity observations; empty is `()`; malformed, duplicate-maturity, or timestamp-mismatched collections are `None`. |

Local precision 50 applies to Decimal arithmetic in `causal_zscore`: mean,
variance, square root, and division. Exact comparisons and `copy_abs()` in
transitions and ranking are context-free and need no `localcontext()` wrapper.
All APIs retain the caller-context guarantee.

Normal decision features, in order, are `z_score`,
`traditional_net_opportunity`, and `reverse_net_opportunity`. Unavailable-data
features are `observation_count`, `gross_excess_spread`, both directional nets,
and calculated `z_score` only when present. Risk flatten has no features.
Reason mappings are `risk_flatten`, `data_flatten`, ordered reversal actions
joined by `_then_`, `remain_flat`, `hold_traditional`, and `hold_reverse`; an
already-flat risk request maps to `risk_flatten_already_flat`.

Ranking excludes a row unless quality and freshness are exact true, count is
`252`, and z-score is non-null. Traditional eligibility is `z >= 2.0` with
traditional net `> 0`; reverse eligibility is `z <= -2.0` with reverse net
`> 0`. It first stable-sorts maturity text ascending, then stable-sorts
`z_score.copy_abs()` descending. This is the P32 provisional convention because
P10 omitted the promised score and tie-break; P10 did not freeze this rule.

## Frozen causal and state evidence

| Fixture economic row | Expected z-score | Production z-score |
| --- | --- | --- |
| `traditional_2y` | `2` | `2` |
| `traditional_5y` | `3` | `3` |
| `reverse_2y` | `-2` | `-2` |
| `reverse_5y` | `-2.6` | `-2.6` |

| State fixture | Prior / input (`z`, traditional net, reverse net, ready, risk) | Expected state / actions | Production state / actions |
| --- | --- | --- | --- |
| `traditional_entry_at_boundary` | `FLAT / 2.0, 0.0001, -0.0001, true, false` | `TRADITIONAL / enter_traditional` | `TRADITIONAL / enter_traditional` |
| `traditional_persistence` | `TRADITIONAL / 1.0, 1, -1, true, false` | `TRADITIONAL / ()` | `TRADITIONAL / ()` |
| `traditional_hysteretic_exit_at_boundary` | `TRADITIONAL / 0.5, 1, -1, true, false` | `FLAT / exit_traditional` | `FLAT / exit_traditional` |
| `reverse_entry_at_boundary` | `FLAT / -2.0, -0.0001, 0.0001, true, false` | `REVERSE / enter_reverse` | `REVERSE / enter_reverse` |
| `traditional_to_reverse_reversal` | `TRADITIONAL / -2.0, -1, 2, true, false` | `REVERSE / exit_traditional, enter_reverse` | `REVERSE / exit_traditional, enter_reverse` |
| `risk_flatten_overrides_entry` | `TRADITIONAL / -3, -2, 2, true, true` | `FLAT / risk_flatten` | `FLAT / risk_flatten` |
| `missing_data_flattens` | `REVERSE / -3, -2, 2, false, false` | `FLAT / data_flatten` | `FLAT / data_flatten` |
| `nonpositive_net_exits` | `REVERSE / -1, 1, 0, true, false` | `FLAT / exit_reverse` | `FLAT / exit_reverse` |

## Ranking and causality evidence

| Case | Literal input / expected | Production |
| --- | --- | --- |
| Mixed eligible | `2Y: 2.1` traditional and `5Y: -3` reverse -> `("5Y", "2Y")` | `("5Y", "2Y")` |
| Absolute-z tie | `5Y: -3`, `2Y: 3` -> `("2Y", "5Y")` | `("2Y", "5Y")` |
| Directional net | `2Y: 9` with traditional net `0`, `5Y: 2` eligible -> `("5Y",)` | `("5Y",)` |
| Exclusions | below threshold, stale, poor quality, missing z, and count `251`, plus eligible `7Y` -> `("7Y",)` | `("7Y",)` |
| Empty / no eligible | `()` and `2Y: 1.9` -> `()` | `()` / `()` |
| Invalid collection | duplicate maturity, unequal timestamps, object, string, or non-observation member -> `None` | `None` in every case |
| Input order | either order of eligible `2Y` / `5Y` -> `("5Y", "2Y")` | `("5Y", "2Y")` in both orders |
| Caller context | precision `2`, nondefault rounding/ranges/capitals/clamp, `Inexact` flag, and `FloatOperation` trap | rank `("5Y", "2Y")`; all settings, flags, and traps unchanged |

Existing focused tests additionally prove warm-up `251 -> None`, `252 -> 2`,
and `253 -> None`; zero variance -> `None`; current/future/reversed/duplicate
history timestamps -> `None`; and perturbing a later excluded row cannot alter
the saved or recomputed causal result. Decision tests cover stale, poor-quality,
mismatched declared-z, short-history, and zero-variance unavailable data. A
valid 252-row history with a null declared z-score uses the computed z-score
and produces the exact ready entry decision.

`signal_pipeline.py` is unchanged by this range (`git diff --
signal_pipeline.py` is empty). It remains the P32-design-labelled legacy
price-residual proxy (`rolling_residual` and proxy-signal functions), separate
from this excess-spread package.

## TDD, simplicity, and verification

The RED command was:

```powershell
& 'C:\Users\jaydo_0v7vk2o\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m unittest docs.tests.test_signal_generation -v
```

It exited `1` with one expected test-module import error: `cannot import name
'rank_opportunities' from 'strategy'`. After the direct function and export,
the same focused command exited `0`, ran `31` tests, and reported `OK`.

The final whole-branch wave added an integrated nullable-declared-z test first.
The targeted command exited `1`: production returned a flat
`data_unavailable` decision instead of the literal traditional-entry decision.
Changing only the readiness predicate to accept a null declaration or an exact
match made the same test pass. The frozen economic oracle now reads each
literal expected z-score from the hashed fixture, direct transition and
integrated decision tests independently snapshot the full nondefault Decimal
context, and reversal coverage asserts `TradeDirection.REVERSE`.

Ponytail full: one existing standard-library module, two stable list sorts,
existing exact-observation validation, and one readiness predicate change; no
dependency, engine, class, configuration object, or tolerance was added.

| Check | Fresh result |
| --- | --- |
| Focused `docs.tests.test_signal_generation` | exit `0`; `36` tests; `OK` |
| Documentation discovery | exit `0`; `245` tests; `OK` (existing `eventkit` deprecation warning) |
| Agent 0 discovery | exit `0`; `25` tests; `OK` |
| `compileall -q strategy docs/tests agents/agent_0` | exit `0`; quiet output |
| Historical, signal, risk, and backtest self-checks | four exit `0` outcomes; each `[OK] self-check passed` |

The fixture hash remained
`3FB9DA5FC9AD255587CE93EA9770552F42566D56070F68AD1661709C030FBD76`.
`git diff --check` exited `0`. Before the code/tests commit, status contained
only `strategy/signal_generation.py` and
`docs/tests/test_signal_generation.py`. No broker, order, or network command is
part of this P32 verification.

## Review status

### Requirements specialist

The requested Luna reviewer was unavailable, so the requirements review used
the Terra-high fallback; no Sol reviewer was used. Verdict: **APPROVE**. The
review independently matched causal z-scores `4/4`, frozen state rows `8/8`,
readiness cases `5/5`, feature mappings `4/4`, and reason mappings `13/13`.
Cross-maturity ranking matches the explicit provisional absolute-z/text-tie
convention. The publication-time/business-calendar gap and P10 ranking-rule
gap remain limitations, not frozen behavior claims.

### Lookahead specialist

The initial verdict was **CHANGES_REQUIRED**: deliberately corrupted model
fields could produce NaN/numeric results or raise instead of failing closed.
Commit `5ca24e9` reuses `_valid_observation`, strengthens `_valid_prior`, and
adds an eight-failure RED plus integrated decision cases. Scoped re-review:
**APPROVE**. Fresh outcomes were focused `33`, documentation discovery `242`,
and Agent 0 `25`, all `OK`.

### Ponytail specialist

The comparison-only `signal_transition` `localcontext()` wrapper was removed
in `b50b441`. `_valid_prior` was retained because it preserves the required
malformed-collection versus valid-but-unavailable distinction. The two stable
ranking sorts were retained because context-safe `copy_abs()` avoids unary
Decimal negation while preserving the deterministic text tie-break. Scoped
Ponytail verdict: **Lean already. Ship.**

### Correctness specialist

The initial verdict was **CHANGES_REQUIRED** only because the Task 2 plan text
mandated a precision-50 wrapper around comparisons. The user-authorized
self-approved design/plan clarification in `b34d1c7` now states that precision
50 applies to arithmetic, while comparisons and `copy_abs()` are context-free.
Scoped re-review: **Spec ✅; APPROVE; no breakage.**

### Senior whole-branch final review

Initial verdict at reviewed pre-fix head `376b067`: **With fixes**. This single
final wave addresses every finding:

- nullable declared z-score incorrectly forced valid computed data unavailable:
  failing integrated RED, one-line readiness fix, and exact GREEN in `b02f315`;
- fixture oracle duplicated a hard-coded profile-to-z map: expected z-scores
  now come directly from the frozen hashed economic rows;
- direct transition and integrated decision context evidence was incomplete:
  independent full nondefault context snapshot tests now prove exact results
  and unchanged live context;
- traditional-to-reverse decision coverage omitted direction: it now asserts
  `TradeDirection.REVERSE`;
- provenance and final counts were stale: this document now distinguishes the
  initial Task 3 range from reviewed head and subsequent fixes, and records the
  fresh final-wave matrix.

Scoped final re-review over `376b067..6327b51`: all five findings **Addressed**
(nullable z, fixture oracle, provenance/counts, transition/generate context,
and reversal direction). No new Critical or Important findings. The reviewer
verified focused `36`, documentation discovery `245`, Agent 0 `25`, and
`git diff --check`, all passing. **Ready to merge: Yes.**

The publication-time/revision cutoff and production business-calendar evidence
remain unavailable in P30, so UTC ordering is not a claim of business-calendar
or publication-time validation. The provisional ranking score/tie-break remains
an explicit P10 gap for future versioned review.
