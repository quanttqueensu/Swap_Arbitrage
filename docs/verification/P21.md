# P21 Verification Record

## Objective and authorization

- Prompt: P21, design narrow schemas and a migration preview.
- Prerequisite: MG2 approved 2026-07-31; committed P20 audit reviewed before
  this task.
- Worktree: `docs/.worktrees/p01`, branch `codex/p10-equations`.
- Design commit: `ed850fd`; plan commits: `da8b5c3`, `7ae6ff8`.
- Schema version: `1.0.0`.
- Requested gate: MG3. No P22 work began.

## Files created or changed

- `data_pipeline/__init__.py`
- `data_pipeline/contracts.py`
- `tests/test_schema_contracts.py`
- `docs/data/canonical-schemas.md`
- `docs/data/migration-preview.md`
- `docs/superpowers/specs/2026-07-31-p21-canonical-schemas-design.md`
- `docs/superpowers/plans/2026-07-31-p21-canonical-schemas.md`
- `docs/verification/P21.md`

The dirty main checkout was not edited. The linked worktree began clean. No
existing source, data, test, configuration, or documentation file was changed.

## TDD evidence

1. Initial RED: `python -m unittest tests.test_schema_contracts -v` failed
   with `ModuleNotFoundError: data_pipeline`.
2. Initial GREEN: 8 schema/catalog/migration tests passed.
3. Requirements corrections added RED coverage for per-column lineage and
   separate paper/backtest schemas; GREEN reached 9 tests.
4. Quality corrections reproduced and fixed acceptance of missing/extra CSV
   cells, compact dates, underscored integers, a blank required paper agent,
   side/quantity contradictions, invalid rate-sensitivity signs, and invalid
   state/direction domains; GREEN reached 13 tests.
5. Specialist corrections reproduced and fixed nonpositive DV01, mismatched
   signal value/unit, invalid trade direction, generic lineage placeholders,
   and an under-specified Eris selection/reconciliation rule; GREEN reached 15
   tests.

Every behavior correction was observed failing for the intended reason before
its implementation was changed.

## Schema and migration evidence

- Nineteen separate narrow schemas are frozen at `1.0.0`.
- Paper and backtest decisions/orders/fills are separate; backtests contain no
  `agent_id` or `ibkr_order_id`.
- Every column has a reason, source/derivation, and at least one named consumer.
- Local validation covers exact headers, row width, canonical scalar lexemes,
  missingness, unique keys, ordering, causal timestamp ordering, proxy
  classification, positive/crossed quotes, DV01/sign/state/direction domains,
  coupled signal value/unit, and signed quantity/side consistency.
- Cross-table contract-validity and decision-cutoff validation is explicitly
  deferred to contextual P22-P24 writer/integration validators rather than
  falsely claimed as a P21 local check.
- All 1,487 P20 artifact headings match exactly one non-executing migration
  rule: 1,474 Eris cache files plus 13 named current artifacts.
- Eris selection freezes exact YIT/YIW quarterly symbol and filename/date
  patterns, blocks ambiguous keys, and requires exact emitted-key/count
  reconciliation.
- No migration action was performed. Raw caches, current wide tables, legacy
  results, and the R2 inventory remain untouched and recoverable.

## Review findings and resolutions

### Requirements review

The reviewer found table-only lineage and shared paper/backtest telemetry.
Resolution: add column-level metadata and split all adapter-specific schemas.

### Quality review

The reviewer found ragged-row acceptance, permissive lexical parsing, an
invalid sample, unsigned side ambiguity, and open sign/state domains.
Resolution: strict row-width and canonical lexical checks, required paper
agent ID, signed side rules, and closed numeric domains with regression tests.
Final result: PASS, focused tests 13/13.

### Schema specialist

The reviewer found missing DV01 positivity, falsely broad cross-table claims,
generic lineage, uncoupled signal fields, and open trade direction.
Resolution: add local rules/tests, field-specific metadata, and explicitly
defer contextual checks. Final result: PASS, focused tests 15/15.

### Migration-safety specialist

The reviewer verified exact one-rule coverage and no destructive path, then
required a deterministic Eris predicate and reconciliation formula.
Resolution: freeze the exact symbol and filename patterns, filename date
parsing, duplicate-key block, and exact key/count equality. Final result: PASS,
focused tests 15/15.

## Requested Ponytail audit

The final whole-repository review was limited to over-engineering and applied
no changes. Ranked findings:

1. `delete:` remove unused `clean_signal_frame`; replacement: nothing.
2. `delete:` remove `clean_existing_derived_csvs` and its test-only surface if
   no external operator still calls it; replacement: nothing.
3. `yagni:` remove constant-false `MigrationRule.performs_action`; a preview
   rule has no mutation method, so structural absence is sufficient.
4. `shrink:` inline one-call `MigrationRule.matches` into
   `migration_rule_for` with `re.fullmatch`.
5. `native:` omit the empty `data_pipeline/__init__.py` on Python 3.12 and use
   native namespace-package behavior.

Estimated net opportunity: about 55 lines and zero dependencies. These are
review suggestions only; P21 does not authorize unrelated cleanup.

## Fresh verification commands

- `.\.venv\Scripts\python.exe -m unittest tests.test_schema_contracts -v`
  - Exit 0; 15 passed, 0 failed.
- `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`
  - Exit 0; 141 passed, 0 failed in 2.703 seconds.
- `.\.venv\Scripts\python.exe -m compileall -q .`
  - Exit 0.
- `.\.venv\Scripts\python.exe raw_price_data.py --self-check`
  - Exit 0; `[OK] self-check passed`.
- `.\.venv\Scripts\python.exe signal_data.py --self-check`
  - Exit 0; `[OK] self-check passed`.
- `.\.venv\Scripts\python.exe risk_data.py --self-check`
  - Exit 0; `[OK] self-check passed`.
- `.\.venv\Scripts\python.exe backtest.py --self-check`
  - Exit 0; `[OK] self-check passed`.
- `git diff --check`
  - Exit 0; no whitespace errors.
- `git status --short`
  - Exit 0; only P21 code/test files appeared because the repository-wide
    `docs` ignore rule hides the new P21 documents until force-staged.

## Data and external systems

- Data read: committed P20 inventory/lineage evidence and governing research
  documents only.
- Real CSV/cache/R2 object bodies read: 0.
- Data files written, moved, rewritten, archived, superseded, or deleted: 0.
- Network, Cloudflare/R2, and IBKR contacts: 0.
- Broker orders submitted, modified, or cancelled during development: 0.

## Known limitations and MG3 approval outcome

P21 freezes contracts and a preview; it does not prove source availability or
perform contextual/cross-table validation. Exact CMS, collateral-consistent
repo, production calendar, forward funding, and 10Y/30Y support remain
unavailable. MG3 review should inspect one synthetic sample per schema, units,
keys, missingness, partitioning, consumers, all four future actions, staging
destinations, and recovery. MG3 approval authorizes staging work only and does
not authorize deletion.

The user explicitly approved MG3 on 2026-08-02 after reviewing the committed
P21 change set and its two noted scope choices: the 19-schema package and the
grouped presentation of 1,474 cache files backed by per-artifact executable
coverage. The gate ledger records that approval. P22 may begin separately,
but approval does not itself stage, move, rewrite, archive, supersede, or
delete any existing source, cache, derived file, or legacy result.

The project remains permanently paper-only. No production endpoint, account,
toggle, or non-paper order path was added.
