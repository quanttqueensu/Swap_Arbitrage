# P24 verification evidence — canonical migration

## Outcome and execution identity

P24 migrated the five approved top-level inputs into validated yearly canonical
partitions without changing any source file. Publication executed the exact code
at immutable commit
`e4d10509e0ae5b6fc537c9b49b6f33a31cdfb112` (publication implementation
`cd6b1bf3c58a80f3d4c89d9769b2e9ac7f4a402f` plus the reviewed present-rate
observation fix). The retained dry runs and published artifacts were produced
offline in the isolated `p23-p24` worktree on 2026-08-02.

The P20 catalog boundary remains explicit: 5 artifacts were consumed and 1,482
were excluded, including 1,474 Eris cache files and the R2 inventory. Those
excluded catalog files were not copied into this worktree and were neither read
nor published by P24.

## Exact commands and phase results

All Python commands used the bundled runtime with the established workspace
site-packages on `PYTHONPATH`:

```powershell
$env:PYTHONPATH='C:\Users\jaydo_0v7vk2o\Downloads\Swap_Arbitrage\.venv\Lib\site-packages'
$py='C:\Users\jaydo_0v7vk2o\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
& $py -m data_pipeline.migration --repo-root . --staging-root data/staging/p24 --report docs/verification/P24-migration-report.csv
& $py -m data_pipeline.migration --repo-root . --staging-root data/staging/p24-repeat --report docs/verification/P24-migration-report-repeat.csv
& $py -m data_pipeline.migration --repo-root . --staging-root data/staging/p24 --report docs/verification/P24-migration-report.csv --publish
```

Both dry runs exited 0. Each retained tree contains 36 files (31 declared
outputs plus 5 private source snapshots); comparison by every relative file
path and SHA-256 found zero mismatches. The reports are byte-identical with
SHA-256
`90d00053a38c74bf75578e93ea8d03ddc43facfb581dfe21336b6e03e5b60941`.
Before publication there were zero canonical destination files and neither
root manifest existed.

Publication began at `2026-08-02T20:00:16.026824Z` and ended at
`2026-08-02T20:00:17.236761Z`. It atomically published exactly 31 declared
files: 9 rate partitions, 7 settlement partitions, 7 risk partitions, 7 daily
market partitions, and `data/manifests/p24_inputs.csv`. No other path was
published. `p24_run.csv` was deliberately absent during staging and
publication; it is finalized only after this evidence exists.

## Immutable input evidence

The pre-run, post-dry-run, and post-publication hashes are identical:

| Input | Rows | Coverage | Bytes | SHA-256 |
| --- | ---: | --- | ---: | --- |
| `data/cme_swap_data.csv` | 2,948 | 2020-09-04..2026-07-15 | 113,986 | `4f7345421c0ced542130b761730513f37a7334c6f8fb507ba2788e6b771775f6` |
| `data/swap_rates.csv` | 1,474 | 2020-09-04..2026-07-15 | 125,622 | `722a0f6558f4673098a06894aa2e1e5d7bbdc66309239659b407102cc6ba5855` |
| `data/treasury_futures.csv` | 1,471 | 2020-09-04..2026-07-15 | 116,115 | `6fded14c4dbeb99a063635adac7b890e4347946de00de422b3746671835fb6c4` |
| `data/treasury_futures_data.csv` | 2,942 | 2020-09-04..2026-07-15 | 118,306 | `c608850478ba0ac46c9e24d96c55b5c79b2576b36182467c40e5079b7ec82718` |
| `data/treasury_rates.csv` | 2,143 | 2018-01-02..2026-07-15 | 177,175 | `d02a86897fabd38c8cf1a4154788abdae95437e01e2b010947c2b21160ad296e` |

The published 5-row input manifest has SHA-256
`b7c4674d0a8019c315fe541a0fb3b9f64a3acef9ceea9c722baff15c9ebb471f`.

## Canonical output counts, coverage, and hashes

All 31 files were reopened and validated against their named schema contracts.
The canonical row total is 26,146.

| Group | Files | Rows | Coverage | Year-file SHA-256 values |
| --- | ---: | ---: | --- | --- |
| rates | 9 | 8,476 | 2018-01-02..2026-07-15 | 2018 `5f211736…`; 2019 `d1ac5fcc…`; 2020 `fa634d0e…`; 2021 `7987c54d…`; 2022 `eaf6eb7c…`; 2023 `6ff3e04a…`; 2024 `eac67429…`; 2025 `7874ff09…`; 2026 `cc724803…` |
| futures settlements | 7 | 5,890 | 2020-09-04..2026-07-15 | 2020 `3d24bcb2…`; 2021 `613656b3…`; 2022 `4e669ec4…`; 2023 `ee66a554…`; 2024 `238e0564…`; 2025 `d7dc8072…`; 2026 `2d7491ac…` |
| contract risk | 7 | 5,890 | 2020-09-04..2026-07-15 | 2020 `c048d94f…`; 2021 `50c13ce0…`; 2022 `8094a690…`; 2023 `d671cd54…`; 2024 `b421749b…`; 2025 `e5260533…`; 2026 `d7290fa8…` |
| daily market | 7 | 5,890 | 2020-09-04..2026-07-15 | 2020 `2ded4e1a…`; 2021 `9bbd9d7e…`; 2022 `87d8f90a…`; 2023 `c28885b1…`; 2024 `8cceb624…`; 2025 `22ac370c…`; 2026 `369d85a1…` |

The migration report is the complete, exact hash ledger for every yearly file.
Its five ordered rows all have `status=pass`, `validation_status=pass`, equal
source/output key counts, and equal independent expected/actual key and value
digests:

| Rule | Reconciled keys | Coverage |
| --- | ---: | --- |
| `cme_swap_master` | 5,896 | 2020-09-04..2026-07-15 |
| `swap_rates` | 2,948 | 2020-09-04..2026-07-15 |
| `treasury_futures` | 2,942 | 2020-09-04..2026-07-15 |
| `treasury_futures_master` | 5,884 | 2020-09-04..2026-07-15 |
| `treasury_rates` | 8,476 | 2018-01-02..2026-07-15 |

## Representative lineage checks

The report contains literal source, independent expected, and reopened actual
objects for the first, middle, and last record of every rule. Representative
checks from that evidence are:

* `cme_swap_master`: 2020-09-04 `YITZ20`, price `99.9254`, DV01 `19.09`
  maps exactly to settlement `ERIS-YIT-202012` and
  `dv01_method=eris_settlement_dv01`.
* `swap_rates`: 2023-08-09 ERIS YIT price
  `92.00200000000001` maps exactly to `ERIS-YIT`, classification `exact`,
  observed at 21:00Z and available at 21:01Z.
* `treasury_futures`: 2026-07-15 ZT value `103.09765625` maps exactly to
  `YAHOO-CONTINUOUS-ZT`, classification `proxy`, with the nonempty label
  `continuous futures proxy`.
* `treasury_futures_master`: 2020-09-04 ZF DV01 `47.58` maps exactly to
  `cme_fixed_ics_ratio_proxy` with rate-sensitivity sign `-1`.
* `treasury_rates`: the absent 2018-01-02 SOFR observation is not fabricated;
  the present EFFR `1.42` maps to 142 bps. The middle SOFR observation maps
  `0.29` to 29 bps, and the last present DGS5 maps `4.26` to 426 bps.

The timing rule is `p24-market-close-v1`, digest
`3d4a5df7dedb5c96291891fa7b3383a0b746d710fa1dcf133beaa089c5c56106`.
Report certainty is `assumed`; ERIS price lineage is `exact`; Yahoo continuous
futures lineage is explicitly `proxy` and never presented as an exchange
settlement.

The run configuration is the UTF-8 SHA-256 of this sorted, compact JSON:
`{"migration_rules":["cme_swap_master","swap_rates","treasury_futures","treasury_futures_master","treasury_rates"],"output_contract_version":"1.0.0","publication_mode":"atomic-no-clobber","timing_matrix_digest":"3d4a5df7dedb5c96291891fa7b3383a0b746d710fa1dcf133beaa089c5c56106","timing_rule_id":"p24-market-close-v1"}`.
Its digest is
`a551497c88bf0978f9c2ce526d5a139d89d57acacbc97686e15d974166ab4fb2`.

## Atomicity, recovery, and safety

Publication revalidates the retained stage/report/source evidence, rejects any
undeclared or existing destination, prepares validated same-parent siblings,
claims all destinations, and rolls back the entire set on failure. Tests cover
stale evidence, late leaf creation, destination-parent replacement, partial
failure rollback, cleanup, and restart publication. Recovery remains the five
untouched top-level originals at the hashes above; the two retained staging
trees also preserve the verified pre-publication artifacts for this execution.

The P24 production modules were scanned for `socket`, common HTTP clients,
IBKR connectivity, `placeOrder`, `reqGlobalCancel`, and `cancelOrder`: every
token count was zero. The real P24 execution performed zero network calls, zero
broker calls, zero order submissions, and zero cancellations.

## Review findings and resolution

Independent review initially found two publication defects. A critical finding
showed that caller-constructed run-manifest metadata could claim false
provenance; the generic finalization helper was removed, and P24 now creates the
manifest only from recorded execution evidence after publication. An important
TOCTOU finding covered destination leaf and parent replacement; publication was
hardened with parent locks/descriptor-relative operations, late-leaf tests, and
rollback. Follow-up POSIX descriptor review and Windows parent-lock review were
clean. A real-data dry run then exposed blank optional rate observations; the
implementation now omits only exact-empty consumed series while retaining
fail-closed validation for malformed, nonfinite, and nonpositive values. Its
focused independent review returned `CLEAN` with no findings. No P24 review
finding remains unresolved. The final whole-package review independently
revalidated both manifests, all 31 published files against both retained
stages, both reports, the 26,146-row total, timestamps, configuration and code
identity, source immutability, evidence completeness, and commit exclusions; it
also returned `CLEAN` with no findings.

The post-commit byte audit then found that Git's repository-wide automatic text
normalization altered only the committed `p24_inputs.csv` blob, which would
break the exact digest recorded above on a platform with different checkout
settings. The file's published bytes were still intact. A path-specific
`data/manifests/p24_inputs.csv -text` rule now preserves this checksum-bearing
manifest byte-for-byte; the re-indexed blob and published file both hash to
`b7c4674d0a8019c315fe541a0fb3b9f64a3acef9ceea9c722baff15c9ebb471f`.
Focused review first rejected the byte-preservation change because bare
`-text` made `git diff --check` treat the preserved CR characters as trailing
whitespace. The final path rule adds `whitespace=cr-at-eol`; exact-byte and
cached/working diff checks then passed and the reviewer returned `CLEAN`.

## Gate request

After this evidence was written, `data/manifests/p24_run.csv` was written to a
validated same-directory temporary file and atomically installed. It validates
as one `run_manifest` row and has SHA-256
`879b5d3499e9f23dc5bdbaa063914621e02657a3be117fac8feab1c5a68bd843`.
It records the exact code and input-manifest hashes, actual publication window,
configuration digest, strategy/run identity, 26,146 canonical rows, and
terminal `complete` status.

Final verification used:

```powershell
& $py -m unittest discover -s tests -v
git -c safe.directory='C:/Users/jaydo_0v7vk2o/Downloads/Swap_Arbitrage/docs/.worktrees/p23-p24' diff --check
```

Result: exit 0; `Ran 209 tests in 11.605s`; `OK`; diff check exit 0 with no
whitespace errors.

P23 and this completed P24 record jointly request **MG4** review. P30 has not
begun and must not begin before MG4 is granted.
