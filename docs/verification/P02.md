# P02 Verification Record

## Prompt ID and objective

**Prompt:** P02 — Characterize and freeze current Agent 0 behavior
**Tasks:** Task 1 characterized current Agent 0 and prepared the MG1
weekly-count decision. Task 2 applies the user's authoritative selection and
completes the pre-review alignment.

Task 1 added fake-only characterization tests, recorded the then-unresolved
weekly-order-count conflict, and made the authorized fail-closed correction to
the mutable `DU` policy. On 2026-07-29 the user selected the already
characterized **5 orders/day × 5 weekdays = 25 orders/week** behavior as
authoritative and conditionally approved MG1. Task 2 freezes that count in the
test name/assertions, corrects current prose and instruments, and preserves the
100/250 proposals as clearly superseded history. Runtime count behavior is
unchanged.

## Status and dependency gates

- The user approved the P01 environment checkpoint on 2026-07-29.
- Work continues on the existing isolated branch `codex/p01-environment`.
- MG0 is approved in this worktree.
- **The authoritative weekly count is 25: 5 orders/day × 5 weekdays.**
- **The user selected that value and conditionally approved MG1 on
  2026-07-29.**
- **Task 2 verification passes; MG1 remains pending until final Task 2
  requirements, quality, and paper-execution-safety reviews pass.**
- **P10 is unstarted and is not authorized by this record.**
- Task 1 requirements, code-quality, and paper-execution-safety reviews passed
  before Task 2 began.

## Code and version state

- Base commit:
  `bf11cef87d102fa1bf88b1e315bc04669275e69e`.
- Task 1 characterization-test commit:
  `8e5f2ea8257b5e12e665477eb127c1f4bd83e2d5`.
- Task 1 evidence commit:
  `a8b202a48bd0913bcc62b8f92af077b23c949836`.
- Characterization quality-fix commit:
  `5b9603c26feb0a9fc0250a33b531bc284f1ec2bc`.
- Task 1 completed at:
  `2a37a94dd45553638d24d977470f8358bbb7b94d`.
- Task 2 base commit:
  `2a37a94dd45553638d24d977470f8358bbb7b94d`.
- Branch: `codex/p01-environment`.
- Config version: none.
- Schema version: none.
- Strategy version: none.
- Agent 0 behavior/config changed: yes, only to make the permanent literal
  `DU` paper-account policy fail closed if its legacy policy fields are
  disabled or changed.
- Agent 0 runtime order count, strategy, and schema changed in Task 2: no.
- Agent 0 current settings prose changed in Task 2: yes, to state the selected
  5/day = 25/week and exact configured instrument set.
- This record intentionally does not predict the hash of its containing Task 2
  commit.

## Files created or changed

Task 1 added `tests/test_agent_0_characterization.py`, added this record, and
modified only `agents/agent_0/config.py` for the fail-closed `DU` correction.

Task 2 changes exactly:

- `tests/test_agent_0_characterization.py`;
- `agents/agent_0/SETTINGS.md`;
- `docs/master-plan/MASTER_PLAN.md`;
- `docs/master-plan/PROMPT_PLAYBOOK.md`;
- `docs/master-plan/VERIFICATION_GATES.md`;
- `docs/superpowers/specs/2026-07-15-agent-0-weekly-orders-design.md`;
- `docs/superpowers/plans/2026-07-15-agent-0-weekly-orders.md`; and
- `docs/verification/P02.md`.

`docs/master-plan/PROJECT_CONTRACTS.md` has no current Agent 0 count claim and
is unchanged. Production Python, `data/`, Agent 0 order directories/CSVs, and
the read-only main-checkout historical originals are unchanged. Ignored
`.superpowers/sdd/P02/task-2-brief.md` and `task-2-report.md` are updated as
working records but are not committed.

## Unrelated pre-existing changes preserved

The isolated worktree was clean at the Task 1 start. The user's main checkout
was read-only and retained this pre-existing state:

```text
 M .gitignore
D  cloudflare_r2_test.py
 M docs/master-plan/MASTER_PLAN.md
 M docs/master-plan/PROJECT_CONTRACTS.md
 M docs/master-plan/PROMPT_PLAYBOOK.md
 M docs/master-plan/VERIFICATION_GATES.md
?? r2_database_names.py
```

The current main-checkout amendments were read for discrepancy analysis only.
They were not copied into or edited from this worktree.

## Current Agent 0 characterization

The tests characterize these current behaviors:

- Missing accounts and accounts without the `DU` prefix are rejected.
- Disabling `REQUIRE_PAPER_ACCOUNT_PREFIX` or changing
  `PAPER_ACCOUNT_PREFIX` is itself rejected before IB class loading/client
  construction, and account validation remains anchored to literal `DU`.
- `PAPER_ONLY=False`, `LIVE_TRADING_ENABLED=True`, or a port other than `7497`
  fails closed before broker-client construction.
- The represented routing is `127.0.0.1:7497`, client ID `30`, with no
  production-routing setting or override in Agent 0.
- The current configuration-derived instrument set is exactly `YIT`, `YIW`,
  `ZT`, and `ZF`. Before Task 2, `SETTINGS.md` additionally named stale `ZN`
  and `ZB`; Task 2 removed them, so current settings and configuration match.
- The same seed and date produce the same weekly plan.
- `next_weekdays` returns exactly five dates, Monday through Friday.
- Current arithmetic is **5 weekdays × 5 orders/day = 25 orders/week**.
- Symbols, sides, quantities, New York timezone, 09:00–15:00 activation
  bounds, scheduled `MKT`/`DAY` fields, account, UTC activation, and
  deterministic order references are checked.
- Allocation spreads one side across exactly three contracts, never above 15
  orders per contract/side, counts existing same-account capacity, ignores
  other-account capacity for this limit, and fails before `placeOrder` when
  the complete remaining allocation does not fit.
- The post-order margin reserve passes exactly at 10%, fails one unit above the
  boundary, fails closed on missing/invalid values, reduces to the largest
  passing quantity, and does not call `placeOrder` when quantity one fails.
- Plan reconciliation preserves matching local state and drops obsolete rows.
- A matching server order reference suppresses duplicate submission.
- A locally accepted reference missing from IBKR is reset and resubmitted.
- Managed-account visibility and submission-order account matching are
  enforced.
- The normal queue path submits only to an in-memory fake and never calls
  cancellation.
- The explicit `--cancel-all` CLI path does not queue a weekly plan, but its
  `reqGlobalCancel` behavior clears every order visible to the session,
  including other-account and manual fake orders.

The normal-queue fake intentionally has no position API. Its successful
end-to-end execution also characterizes that the current queue does not inspect
positions.

## Weekly-order selection and historical source table

| Count | Status | Exact source and interpretation |
|---:|---|---|
| **25** | **Authoritative selection, 2026-07-29** | `agents/agent_0/config.py` sets `ORDERS_PER_DAY = 5`; `next_weekdays` creates five weekdays; the renamed focused test `test_authoritative_plan_has_five_orders_per_day_and_25_per_week` independently asserts 5/day, five dates, the literal `5 * 5` arithmetic, and 25 total. |
| **50** | Historical current-prose arithmetic error, corrected | Before Task 2, `SETTINGS.md` and the master-plan audit paired 5/day with “50/week.” This never described runtime arithmetic. Task 2 corrects those current claims while preserving this chronology here. |
| **100** | Superseded historical design/proposal | The historical design proposed 20/day = 100/week; the later portion of the historical implementation plan proposed the same reduction. Both remain historical beneath a superseded notice. |
| **250** | Superseded historical implementation proposal | The historical implementation plan initially proposed 50/day = 250/week. It remains historical beneath a superseded notice. |

**Selection rationale:** 25/week preserves the already characterized runtime
and makes the arithmetic exact across the five Monday-through-Friday target
dates. It resolves the prose disagreement without changing production
behavior, random selection, risk limits, routing, or order submission.

The immutable historical-source references were captured with:

```powershell
$paths = @(
  'C:\Users\jaydo_0v7vk2o\Downloads\Swap_Arbitrage\docs\superpowers\specs\2026-07-15-agent-0-weekly-orders-design.md',
  'C:\Users\jaydo_0v7vk2o\Downloads\Swap_Arbitrage\docs\superpowers\plans\2026-07-15-agent-0-weekly-orders.md'
)
foreach ($path in $paths) {
  $hash = Get-FileHash -Algorithm SHA256 -LiteralPath $path
  "PATH=$($hash.Path)"
  "SHA256=$($hash.Hash.ToLowerInvariant())"
  "BYTES=$((Get-Item -LiteralPath $path).Length)"
}
```

Exit 0. Task 1 used these source hashes to establish the historical
chronology. Task 2 copied the records into the branch with top superseded
notices and preservation proof recorded below; the main-checkout originals
remain read-only and unchanged.

### Task 2 historical-preservation proof

The design copy has one top superseded notice. Removing that notice in memory
produces a byte-for-byte match with the read-only source:

| Artifact | Source bytes | Source SHA-256 | Compared body bytes | Compared body SHA-256 | Result |
|---|---:|---|---:|---|---|
| Historical design | 9,017 | `1eb90e4b03ada940cf050a88ba9d25238b8ec3def4a457ccc5b771938e4ab2c1` | 9,017 | `1eb90e4b03ada940cf050a88ba9d25238b8ec3def4a457ccc5b771938e4ab2c1` | byte-identical |
| Historical implementation plan | 33,488 | `bb443aacd1cc2006e4b7007c48d4d238774fcd33d90aadd17a17d7abe7e8863d` | 33,490 | `5d939c8884518d95831b5fb18b5a297a2f8c7502db132b08b25852c0081a42b1` | normalized body identical |

The implementation-plan comparison normalizes exactly one historical
paper-account identifier in memory to `DU_REDACTED`, matching the branch copy,
before hashing and comparing. Redaction count: **1**. Normalized source bytes:
33,490. Normalized source SHA-256:
`5d939c8884518d95831b5fb18b5a297a2f8c7502db132b08b25852c0081a42b1`.
The normalized source and banner-stripped branch body are byte-identical. No
original account value is recorded in this branch, command output, or report.

The preservation command:

1. reads each source and branch copy using strict UTF-8;
2. validates and removes the exact top notice in memory;
3. applies the one-count account redaction to the source plan in memory;
4. computes SHA-256 and byte counts; and
5. exits nonzero unless the design body and normalized plan body match.

Observed result: exit `0`,
`DESIGN_BODY_IDENTICAL=True`,
`PLAN_REDACTION_COUNT=1`, and
`PLAN_NORMALIZED_BODY_IDENTICAL=True`.

## Source and ledger conflict table

| Topic | Worktree / implemented evidence | Read-only main-checkout or prose evidence | P02 treatment/resolution |
|---|---|---|---|
| MG0 | The `Gate ledger` / `MG0` row in `docs/master-plan/VERIFICATION_GATES.md` records `Approved 2026-07-28`; the user also approved the P01 checkpoint on 2026-07-29. | The amended main-checkout gate ledger says MG0 `Not started`. | Treat the worktree/user approval as controlling for this branch; do not edit the main checkout. |
| Weekly total | Executed behavior is 25. | Current prose formerly said 50; historical sources propose 100 and 250. | Resolved 2026-07-29: 25/week is authoritative; current prose is corrected and 100/250 remain superseded history. |
| Instrument set | Current project config provides exactly `YIT`, `YIW`, `ZT`, `ZF`. | `SETTINGS.md` formerly added stale `ZN` and `ZB`. | Resolved in Task 2 by making `SETTINGS.md` match the configuration-derived set exactly. |
| Cancellation isolation | `agents/agent_0/broker.py:66` calls `reqGlobalCancel()` with no account/order-reference filter. | The historical design correctly describes session-wide scope. | Record a known limitation, not a passing account-isolation property. |
| Default execution | `run.py` defaults to `queue_next_week`, which can submit transmitted paper orders after connection. | The later master verification standard calls for a default dry run. | Characterize the current non-dry-run behavior with fakes; do not rewrite production behavior in Task 1. |

The main-checkout master-plan amendments primarily expand future Phase 1
equation/source checkpoints. They do not authorize P10 or change this bounded
P02 task.

## Test inventory mapped to requirements

| Requirement | Characterization tests |
|---|---|
| Missing/non-`DU` account rejection | `test_missing_and_non_paper_accounts_are_rejected` |
| Immutable literal-`DU` policy and pre-client rejection | `test_disabled_du_policy_is_rejected_before_ib_class_loading`; `test_changed_du_prefix_is_rejected_before_ib_class_loading` |
| Fail-closed paper flags and port | `test_paper_flags_and_port_fail_closed`; `test_unsafe_port_is_rejected_before_a_client_is_constructed` |
| Exact host/port/client and no production routing | `test_connection_uses_only_current_paper_host_port_and_client` |
| Managed-account and submission-account checks | `test_managed_account_and_order_account_must_match` |
| Guard active before Agent 0 imports | `test_agent_modules_are_imported_under_already_active_guard`; production imports occur after a module-lifetime barrier proves active, so a future import-time connection attempt fails collection closed |
| Hard socket/network guard through tests | All classes inherit `SocketGuardedTestCase`, whose `setUp` reasserts the module guard; `test_socket_guard_blocks_direct_connection_attempts` proves direct attempts are blocked |
| Current allowed-instrument set | `test_allowed_instruments_are_derived_from_current_configuration` |
| Five target weekdays | `test_next_week_has_exactly_monday_through_friday` |
| Determinism and authoritative 5/day = 25/week | `test_authoritative_plan_has_five_orders_per_day_and_25_per_week` |
| Symbol, side, quantity, timezone, activation and ref bounds | `test_plan_fields_stay_within_current_symbol_side_quantity_and_time_bounds` |
| Scheduled order/account fields | `test_scheduled_market_order_preserves_account_reference_and_utc_activation` |
| Three contracts and independent 15-per-side caps | `test_three_contract_allocation_has_independent_fifteen_order_side_buckets` asserts `CONTRACTS_PER_SYMBOL == 3`, then exactly 15 BUY and 15 SELL records in each of six contract/side buckets |
| Existing capacity accounting | `test_existing_account_orders_consume_capacity_but_other_accounts_do_not` |
| Preflight before submission | `test_capacity_failure_occurs_before_any_order_submission` |
| 10% reserve boundary and fail-closed values | `test_margin_reserve_uses_exact_ten_percent_boundary_and_fails_closed` |
| Quantity reduction | `test_margin_preview_reduces_to_largest_passing_quantity` |
| Quantity-one block without `placeOrder` | `test_quantity_one_margin_failure_never_calls_place_order` |
| Plan reconciliation | `test_plan_reconciliation_preserves_matching_state_and_drops_obsolete_rows` |
| Server-authoritative duplicate suppression | `test_server_reference_is_authoritative_and_suppresses_duplicate_submission` |
| Accepted-local/missing-server resubmission | `test_local_acceptance_missing_from_server_is_reset_and_resubmitted` |
| Normal queue cancellation separation and no position check | `test_normal_queue_uses_fake_broker_and_never_invokes_global_cancel` |
| Explicit CLI separation and session-wide global scope | `test_explicit_cancel_cli_is_separate_but_session_wide` |

## Fake-only and filesystem-isolation evidence

The module first imports `ib_insync` only to complete eventkit's documented
local asyncio self-pipe initialization. It then starts one module-lifetime
network barrier, proves a direct connection raises, and only afterward imports
any `agents.agent_0` production module. If a present or future Agent 0 import
attempts to connect, name-resolve, send UDP, or call the asyncio connection
boundary, module collection fails closed. The barrier remains active until
`tearDownModule`.

Every test class derives from `SocketGuardedTestCase`. Its `setUp` proves that
the same module-lifetime barrier is still active before each test. The barrier
blocks:

- `socket.socket.connect`;
- `socket.socket.connect_ex`;
- `socket.socket.sendto`;
- `socket.create_connection`;
- `socket.getaddrinfo`; and
- `asyncio.BaseEventLoop.create_connection`.

No Agent 0 production module is imported before the barrier. No broker object
is instantiated and no external connection occurs during the local-only
`ib_insync` initialization. Every Agent 0 import, test body, broker fake, queue
invocation, cancellation invocation, and order construction runs while the
barrier is active.

The tests call real Agent 0 pure/orchestration behavior with purpose-built
in-memory broker/trade/order objects. Broker contract resolution and
connect/disconnect boundaries are replaced only where they would be external.
All CSV operations use `TemporaryDirectory` paths. The normal queue patches
both the dynamic config paths and the `submit_plan` default-argument boundary,
then the command wrapper asserts `agents/agent_0/orders` remains absent.

Per final focused/full Agent 0 run:

- fake `placeOrder` calls: 26 (25 normal-queue orders plus one intentional
  missing-server resubmission);
- fake `reqGlobalCancel` calls: 1;
- real broker/network calls: 0;
- repository order-file writes: 0.

## Tests written before implementation

The original characterization tests preceded no production implementation and
passed against existing behavior. The later paper-safety correction followed a
strict RED/GREEN cycle: its two regression tests were added and observed
failing before `agents/agent_0/config.py` changed.

For Task 2, the existing count characterization was renamed and strengthened
*before* settings or current design prose changed. The selected value equals
the existing runtime value, so the updated characterization correctly passed
immediately: no production behavior was missing and no artificial RED was
created. The guarded focused run reported 25/25 passing, with the authoritative
count test asserting 5 orders/day, five weekdays, `5 * 5`, and 25 total.

Development runs also validated the harness itself:

| Command/run | Exit | Wall time | Outcome |
|---|---:|---:|---|
| Baseline `.\.venv\Scripts\python.exe -m unittest discover -s tests -v` | 0 | 1004 ms | `Ran 29 tests in 0.296s`; `OK`. |
| Initial mistaken focused module path | 1 | 140 ms | `Ran 1 test`; import error because top-level `tests` is not a package. Test file moved to the canonical top-level test directory. |
| First real focused run | 1 | 825 ms | `Ran 22 tests in 0.122s`; 6 failures. The hard socket guard correctly intercepted `ib_insync`'s first-import local asyncio self-pipe initialization. |
| Focused run after dependency initialization fix | 1 | 851 ms | `Ran 22 tests in 0.103s`; 1 failure. This exposed that `submit_plan` had captured repository CSV defaults at function definition time. |
| Original characterization GREEN run (pre-quality and pre-paper-safety) | 0 | 841 ms | `Ran 22 tests in 0.091s`; `OK`; explicit post-run check confirmed the worktree order directory was absent. |
| Quality-fix guarded-import RED run | 1 | 843 ms | `Ran 23 tests in 0.085s`; exactly one expected failure: `_AGENT_IMPORT_GUARD_ACTIVE` was false because Agent 0 imports still preceded the barrier. |
| Quality-fix guarded-import GREEN run | 0 | 826 ms | `Ran 23 tests in 0.072s`; `OK`; Agent 0 imports moved under the module barrier. |
| Quality-fix allocation GREEN run | 0 | 822 ms | `Ran 23 tests in 0.068s`; `OK`; three configured contracts and six exact BUY/SELL buckets characterized. |
| Paper-safety RED run | 1 | 844 ms | `Ran 25 tests in 0.090s`; exactly the two mutable-policy regressions failed because no `RuntimeError` was raised. |
| Paper-safety GREEN run | 0 | 830 ms | `Ran 25 tests in 0.076s`; `OK`; literal-`DU` policy mutation fails before IB class loading. |

During the one-failure harness run, a newly created ignored
worktree-local `agents/agent_0/orders/upcoming.csv` was written because of the
captured default path. It did not exist at baseline, did not overwrite a
pre-existing file, and was immediately identified and removed after its
resolved path was verified inside the isolated worktree. The test now injects
explicit temporary paths at that boundary. The successful focused and full
runs both confirmed no worktree order directory remained or was recreated.
The main checkout and every external system were untouched.

## Command evidence by checkpoint

All final commands ran from:

```text
C:\Users\jaydo_0v7vk2o\Downloads\Swap_Arbitrage\docs\.worktrees\p01
```

using the worktree `.venv`. The first table is the historical original
characterization checkpoint; the quality-fix and paper-safety tables below
supersede its focused/full counts.

| Command | Exit | Wall time | Exact outcome |
|---|---:|---:|---|
| `.\.venv\Scripts\python.exe -m unittest discover -s tests -p 'test_agent_0_characterization.py' -v` | 0 | 841 ms | `Ran 22 tests in 0.091s`; `OK`. |
| `.\.venv\Scripts\python.exe -m unittest discover -s tests -v` | 0 | 1089 ms | `Ran 51 tests in 0.324s`; `OK` (51 passed, 0 failures/errors). Post-run order-directory check: absent. |
| Conditional `compileall` procedure from P01 | 0 | 98 ms | Quiet success for `agents`, `tests`, all tracked entry points, and present `cloudflare_r2_test.py`. |
| `.\.venv\Scripts\python.exe raw_price_data.py --self-check` | 0 | 546 ms | `[OK] self-check passed`. |
| `.\.venv\Scripts\python.exe signal_data.py --self-check` | 0 | 523 ms | `[OK] self-check passed`. |
| `.\.venv\Scripts\python.exe risk_data.py --self-check` | 0 | 571 ms | `[OK] self-check passed`. |
| `.\.venv\Scripts\python.exe backtest.py --self-check` | 0 | 553 ms | `[OK] self-check passed`. |
| `.\.venv\Scripts\python.exe -m pip check` | 0 | 431 ms | `No broken requirements found.` |
| Task-file secret/account-value scan | 0 | 189 ms | Scanned 2 files with 4 high-confidence rules; 0 matches. Environment-variable names and fake `DU_TEST` aliases are permitted; no value was read. |
| `git diff --cached --check` | 0 | 48 ms | No staged whitespace errors. |
| `git diff --check` | 0 | 45 ms | No unstaged whitespace errors. |
| `git status --short` | 0 | 50 ms | Before the evidence commit, only `A  docs/verification/P02.md` remained. |
| Staged-path scope assertion | 0 | 45 ms | Exactly one staged path: `docs/verification/P02.md`; worktree Agent 0 order directory absent. |

The quality-review fix wave supersedes the earlier focused/full counts:

| Command | Exit | Wall time | Exact outcome |
|---|---:|---:|---|
| Guarded focused wrapper below | 0 | 839 ms | `Ran 23 tests in 0.074s`; `OK`; Agent 0 imports and every test ran under the module barrier; order directory absent. |
| Guarded full wrapper below | 0 | 1070 ms | `Ran 52 tests in 0.312s`; `OK` (52 passed, 0 failures/errors); order directory absent. |
| Conditional `compileall` procedure | 0 | 97 ms | Quiet success for the same targets listed below. |
| `raw_price_data.py --self-check` | 0 | 554 ms | `[OK] self-check passed`. |
| `signal_data.py --self-check` | 0 | 530 ms | `[OK] self-check passed`. |
| `risk_data.py --self-check` | 0 | 579 ms | `[OK] self-check passed`. |
| `backtest.py --self-check` | 0 | 558 ms | `[OK] self-check passed`. |
| `python -m pip check` | 0 | 433 ms | `No broken requirements found.` |
| Exact secret/account scan below | 0 | 182 ms | 2 committed Task 1 files scanned; 4 rules; 0 matches. |

The paper-safety correction wave supersedes the focused/full counts above:

The RED focused run used the same guarded wrapper with only the two regression
tests added: exit 1, wall 844 ms, `Ran 25 tests in 0.090s`, exactly two
failures. Both failures said `RuntimeError not raised`; the other 23 tests
passed and the order directory remained absent. This directly reproduced both
mutable-policy bypasses before `agents/agent_0/config.py` changed.

| Command | Exit | Wall time | Exact outcome |
|---|---:|---:|---|
| Guarded focused wrapper below | 0 | 845 ms | `Ran 25 tests in 0.077s`; `OK`; both unsafe policy mutations failed before `_load_ib_class`, normal `DU_TEST` fake routing passed, and the order directory was absent. |
| Guarded full wrapper below | 0 | 1084 ms | `Ran 54 tests in 0.316s`; `OK` (54 passed, 0 failures/errors); order directory absent. |
| Conditional `compileall` procedure | 0 | 274 ms | Quiet success for the same targets listed below. |
| `raw_price_data.py --self-check` | 0 | 549 ms | `[OK] self-check passed`. |
| `signal_data.py --self-check` | 0 | 529 ms | `[OK] self-check passed`. |
| `risk_data.py --self-check` | 0 | 572 ms | `[OK] self-check passed`. |
| `backtest.py --self-check` | 0 | 541 ms | `[OK] self-check passed`. |
| `python -m pip check` | 0 | 419 ms | `No broken requirements found.` |
| Exact secret/account scan below | 0 | 190 ms | 3 committed P02-owned files scanned; 4 rules; 0 matches. |

The exact focused wrapper was:

```powershell
$sw = [System.Diagnostics.Stopwatch]::StartNew()
& .\.venv\Scripts\python.exe -m unittest discover -s tests `
  -p 'test_agent_0_characterization.py' -v
$code = $LASTEXITCODE
$sw.Stop()
"EXIT_CODE=$code"
"ELAPSED_MS=$($sw.ElapsedMilliseconds)"
if (Test-Path -LiteralPath 'agents\agent_0\orders') {
  'UNEXPECTED_REPOSITORY_ORDER_DIR_PRESENT'
  exit 97
}
exit $code
```

The exact full-suite wrapper was:

```powershell
$sw = [System.Diagnostics.Stopwatch]::StartNew()
& .\.venv\Scripts\python.exe -m unittest discover -s tests -v
$code = $LASTEXITCODE
$sw.Stop()
"EXIT_CODE=$code"
"ELAPSED_MS=$($sw.ElapsedMilliseconds)"
if (Test-Path -LiteralPath 'agents\agent_0\orders') {
  'UNEXPECTED_REPOSITORY_ORDER_DIR_PRESENT'
  exit 97
}
exit $code
```

The exact conditional compilation procedure was:

```powershell
$targets = @(
  'agents', 'tests', 'backtest.py', 'config.py', 'data_io.py',
  'raw_price_data.py', 'risk_data.py', 'signal_data.py'
)
if (Test-Path -LiteralPath 'cloudflare_r2_test.py' -PathType Leaf) {
  $targets += 'cloudflare_r2_test.py'
}
& .\.venv\Scripts\python.exe -m compileall -q @targets
```

The exact secret/account hygiene command was:

```powershell
$paths = @(
  'agents\agent_0\config.py',
  'tests\test_agent_0_characterization.py',
  'docs\verification\P02.md'
)
$rules = [ordered]@{
  LikelyPaperAccount = '(?<![A-Z0-9])DU[A-Z0-9]{6,}(?![A-Z0-9])'
  AwsAccessKey = 'AKIA[0-9A-Z]{16}'
  PrivateKey = '-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----'
  CredentialAssignment = '(?i)(?:token|secret|password|api[_-]?key)\s*[=:]\s*["''][^"'']{8,}["'']'
}
$sw = [System.Diagnostics.Stopwatch]::StartNew()
$hits = @()
foreach ($path in $paths) {
  $text = Get-Content -Raw -LiteralPath $path
  foreach ($entry in $rules.GetEnumerator()) {
    if ([regex]::IsMatch($text, $entry.Value)) {
      $hits += "$path::$($entry.Key)"
    }
  }
}
$sw.Stop()
"SCANNED_FILES=$($paths.Count)"
"MATCH_COUNT=$($hits.Count)"
if ($hits.Count) { $hits }
"EXIT_CODE=$([int]($hits.Count -gt 0))"
"ELAPSED_MS=$($sw.ElapsedMilliseconds)"
if ($hits.Count) { exit 1 }
```

It examines only the three committed P02-owned files and emits rule
labels/counts, never matching text or reading an environment value.

### Task 2 final verification

All commands ran from the isolated P02 worktree using its `.venv`. The guarded
test wrappers exit `97` if `agents/agent_0/orders` exists after a run.

| Command/check | Exit | Wall time | Exact outcome |
|---|---:|---:|---|
| Guarded focused Agent 0 discovery | 0 | 972 ms | `Ran 25 tests in 0.078s`; `OK`; authoritative count test passed; order directory absent. |
| Guarded canonical full suite | 0 | 1,235 ms | `Ran 54 tests in 0.401s`; `OK` (54 passed, 0 failures/errors); order directory absent. |
| Conditional `compileall` procedure | 0 | 119 ms | Quiet success for `agents`, `tests`, all tracked entry points, and the present optional Cloudflare entry point. |
| `raw_price_data.py --self-check` | 0 | 608 ms | `[OK] self-check passed`. |
| `signal_data.py --self-check` | 0 | 667 ms | `[OK] self-check passed`. |
| `risk_data.py --self-check` | 0 | 678 ms | `[OK] self-check passed`. |
| `backtest.py --self-check` | 0 | 712 ms | `[OK] self-check passed`. |
| `python -m pip check` | 0 | 1,113 ms | `No broken requirements found.` |
| Markdown/UTF-8/merge-marker/secret/account/current-claim hygiene | 0 | 221 ms; Appendix A.1 | 10 Task 2 files scanned, including 9 Markdown files; 1 permitted synthetic account-pattern match; 0 errors. The historical real identifier is absent. |
| Historical normalized-body proof | 0 | 43 ms; Appendix A.2 | Design 9,017/9,017 bytes with 0 mismatches; plan source 33,488 bytes, redaction count 1, normalized/body 33,490/33,490 bytes with 0 mismatches; 0 errors. |
| Explicit Task 2 and first correction ranges, clean status, and order-directory assertion | 0 | 183 ms; Appendix A.3 | Task 2 path count 8 and first committed correction path count 1, both with 0 scope mismatches; clean tracked status; order directory absent; 0 errors. |

The focused count characterization was updated first and immediately passed
because 25/week was already runtime behavior. This is intentional
characterization-first evidence, not a skipped behavior-change RED.

### Appendix A — exact timed custom checks

Each count-only command was run exactly as shown. No command emits matched
secret or account text.

#### A.1 — 10-file hygiene

```powershell
$p=@('agents/agent_0/SETTINGS.md','docs/master-plan/MASTER_PLAN.md','docs/master-plan/PROMPT_PLAYBOOK.md','docs/master-plan/VERIFICATION_GATES.md','docs/superpowers/plans/2026-07-15-agent-0-weekly-orders.md','docs/superpowers/specs/2026-07-15-agent-0-weekly-orders-design.md','docs/verification/P02.md','tests/test_agent_0_characterization.py','.superpowers/sdd/P02/task-2-brief.md','.superpowers/sdd/P02/task-2-report.md')
$u=[Text.UTF8Encoding]::new($false,$true); $r=@('AKIA[0-9A-Z]{16}','-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----','(?i)(?:token|secret|password|api[_-]?key)\s*[=:]\s*["''][^"'']{8,}["'']'); $sw=[Diagnostics.Stopwatch]::StartNew(); $e=0; $md=0; $am=0
foreach($f in $p){try{$t=$u.GetString([IO.File]::ReadAllBytes((Resolve-Path -LiteralPath $f)))}catch{$e++;continue}; $e += [int]($t.Contains([char]0) -or $t-match'(?m)^(<<<<<<<|=======|>>>>>>>)'); if($f.EndsWith('.md')){$md++;$e += [int](([regex]::Matches($t,'(?m)^`{3,}')).Count%2-ne 0)}; foreach($x in $r){$e += [int]([regex]::IsMatch($t,$x))}; foreach($m in [regex]::Matches($t,'(?<![A-Z0-9_])DU[A-Z0-9]{6,}(?![A-Z0-9_])')){$am++;$e += [int]($m.Value-notmatch'^DU(?:1234567)$')}}
$s=Get-Content -Raw -Encoding utf8 'agents/agent_0/SETTINGS.md'; $e += [int]($s-match'50.?total|\bZN\b|\bZB\b'); $sw.Stop()
"SCANNED_FILES=$($p.Count)"; "MARKDOWN_FILES=$md"; "ACCOUNT_PATTERN_MATCHES=$am"; "ERROR_COUNT=$e"; "ELAPSED_MS=$($sw.ElapsedMilliseconds)"; if($e){exit 1}
```

Result: exit `0` in **221 ms**; scanned `10` files (`9` Markdown), found `1`
permitted synthetic account-pattern match and `0` errors.

#### A.2 — historical body preservation

```powershell
$w=git rev-parse --show-toplevel; $m=Split-Path -Parent (git rev-parse --git-common-dir); $u=[Text.UTF8Encoding]::new($false,$true); $sw=[Diagnostics.Stopwatch]::StartNew()
$ds=[IO.File]::ReadAllBytes("$m/docs/superpowers/specs/2026-07-15-agent-0-weekly-orders-design.md"); $dt=[IO.File]::ReadAllText("$w/docs/superpowers/specs/2026-07-15-agent-0-weekly-orders-design.md",$u); $di=$dt.IndexOf("# Agent 0 Weekly Orders Design`n"); $db=if($di-gt 0){$u.GetBytes($dt.Substring($di))}else{[byte[]]::new(0)}
$ps=[IO.File]::ReadAllBytes("$m/docs/superpowers/plans/2026-07-15-agent-0-weekly-orders.md"); $pt=$u.GetString($ps); $ap='(?<prefix>[.]venv\\Scripts\\python[.]exe agents\\agent_0\\run[.]py --cancel-all --account )DU[A-Z][A-Z0-9]+'; $rc=[regex]::Matches($pt,$ap).Count; $pn=$u.GetBytes([regex]::Replace($pt,$ap,'${prefix}DU_REDACTED'))
$bt=[IO.File]::ReadAllText("$w/docs/superpowers/plans/2026-07-15-agent-0-weekly-orders.md",$u); $bi=$bt.IndexOf("# Agent 0 Weekly Orders Implementation Plan`n"); $pb=if($bi-gt 0){$u.GetBytes($bt.Substring($bi))}else{[byte[]]::new(0)}; $dm=[int](-not[Linq.Enumerable]::SequenceEqual([byte[]]$ds,[byte[]]$db)); $pm=[int](-not[Linq.Enumerable]::SequenceEqual([byte[]]$pn,[byte[]]$pb)); $e=[int]($rc-ne 1)+$dm+$pm; $sw.Stop()
"DESIGN_SOURCE_BYTES=$($ds.Length)"; "DESIGN_BODY_BYTES=$($db.Length)"; "DESIGN_MISMATCH_COUNT=$dm"; "PLAN_SOURCE_BYTES=$($ps.Length)"; "PLAN_REDACTION_COUNT=$rc"; "PLAN_NORMALIZED_BYTES=$($pn.Length)"; "PLAN_BODY_BYTES=$($pb.Length)"; "PLAN_MISMATCH_COUNT=$pm"; "ERROR_COUNT=$e"; "ELAPSED_MS=$($sw.ElapsedMilliseconds)"; if($e){exit 1}
```

Result: exit `0` in **43 ms**; design bytes `9017/9017`, mismatch count `0`;
plan source bytes `33488`, redaction count `1`, normalized/body bytes
`33490/33490`, mismatch count `0`, total errors `0`.

#### A.3 — committed ranges, clean status, and order-directory absence

```powershell
$ErrorActionPreference='Stop'; $tr='2a37a94dd45553638d24d977470f8358bbb7b94d..4eee7a8c8165ddc9bbfe1f4786c22a2248a14acb'; $cr='4eee7a8c8165ddc9bbfe1f4786c22a2248a14acb..84e325513c62a1f14e1c64d2a67f1d721674ed9c'
$et=@('agents/agent_0/SETTINGS.md','docs/master-plan/MASTER_PLAN.md','docs/master-plan/PROMPT_PLAYBOOK.md','docs/master-plan/VERIFICATION_GATES.md','docs/superpowers/plans/2026-07-15-agent-0-weekly-orders.md','docs/superpowers/specs/2026-07-15-agent-0-weekly-orders-design.md','docs/verification/P02.md','tests/test_agent_0_characterization.py')|Sort-Object; $ec=@('docs/verification/P02.md'); $sw=[Diagnostics.Stopwatch]::StartNew()
$at=@(git diff --name-only $tr); if($LASTEXITCODE-ne 0){throw 'Task 2 range diff failed'}; $at=$at|Sort-Object
$ac=@(git diff --name-only $cr); if($LASTEXITCODE-ne 0){throw 'Correction range diff failed'}; $ac=$ac|Sort-Object
$st=@(git status --short); if($LASTEXITCODE-ne 0){throw 'Git status failed'}
$tm=@(Compare-Object $et $at).Count; $cm=@(Compare-Object $ec $ac).Count; $od=[int](Test-Path -LiteralPath 'agents/agent_0/orders'); $e=$tm+$cm+$st.Count+$od; $sw.Stop()
"TASK2_PATH_COUNT=$($at.Count)"; "TASK2_SCOPE_MISMATCH_COUNT=$tm"; "CORRECTION1_PATH_COUNT=$($ac.Count)"; "CORRECTION1_SCOPE_MISMATCH_COUNT=$cm"; "TRACKED_STATUS_COUNT=$($st.Count)"; "ORDER_DIR_PRESENT=$od"; "ERROR_COUNT=$e"; "ELAPSED_MS=$($sw.ElapsedMilliseconds)"; if($e){exit 1}
```

Result: exit `0` in **183 ms**; Task 2 path count `8`, first committed
correction path count `1`, both scope mismatch counts `0`, tracked status count
`0`, order-directory-present count `0`, and total errors `0`.

## Golden or manual calculations checked

No strategy equation or golden trading calculation is in P02 Task 1 scope.
The hand calculation checked is:

```text
5 orders/day × 5 weekdays = 25 orders/week
```

This agrees with the actual plan generator, selected setting, and focused test.
The former prose total of 50 is retained only as a resolved historical error.

## Data read or changed

Read:

- all four worktree master-plan files;
- every Agent 0 Python module and `SETTINGS.md`;
- P00 and P01 evidence;
- the two historical weekly-order plan/design documents in the main checkout;
- the main-checkout master-plan amendments through a full worktree-to-main
  discrepancy diff;
- source/configuration needed for current behavior characterization.

No market/source/result data, existing Agent order CSV, `.env` value, broker
state, or external endpoint was read.

Task 2 changed only the eight tracked paths listed in “Files created or
changed.” The historical sources were read-only inputs. Their branch copies
contain only top notices plus the preserved historical bodies; one historical
paper-account identifier in the plan copy is replaced with `DU_REDACTED`.
The temporary Task 1 harness incident and cleanup remain disclosed above; no
order file or `agents/agent_0/orders` directory is present in the final
worktree.

## External systems contacted

- IBKR connections: 0.
- Broker-order submissions: 0.
- Broker cancellations: 0.
- Cloudflare/R2 calls: 0.
- Public market-data requests: 0.
- Package-index operations: 0.
- Other network/socket connections: 0.

**Broker-order submission count during all P02 development: 0.**

## Review status

- Task 1 implementer self-review: complete.
- Task 1 requirements review: **PASS**.
- Task 1 code-quality review: **PASS** after one correction round.
- Task 1 broker/paper-execution-safety review: **PASS** after the fail-closed
  `DU` correction and one documentation-only correction round.
- Task 2 implementer self-review: **PASS**; the eight-path staged diff matches
  the selected count, changes no production Python, preserves both historical
  bodies subject only to the disclosed one-count security redaction, keeps MG1
  pending, and does not start P10.
- Task 2 final review set: documentation/evidence-only correction findings
  received and addressed in this round; controller re-review is pending.
- MG1: **conditional user approval received and Task 2 verification passed;
  effective ledger approval awaits the three final Task 2 reviews**.

No pending review is represented as a pass in advance.

### Requirements review findings and resolutions

Task 1: **PASS**. Task 2's final review identified evidence-chronology,
citation, and reproducibility corrections; this round addresses them without a
product or gate change. Controller re-review is pending.

### Quality review findings and resolutions

Initial result: **FAIL**.

1. **HIGH — imports preceded the `setUp` guard.** Resolved by pre-initializing
   only the local `ib_insync` asyncio self-pipe, starting/proving a
   module-lifetime network barrier, importing every Agent 0 production module
   only under that barrier, keeping it active through `tearDownModule`, and
   adding `test_agent_modules_are_imported_under_already_active_guard`.
2. **MEDIUM — allocation covered supplied contracts and BUY only.** Resolved
   by asserting `config.CONTRACTS_PER_SYMBOL == 3`, allocating 45 BUY and 45
   SELL records, and requiring six independent 15-order buckets. The
   same-side 46th-order preflight test remains.
3. **LOW — evidence was not fully reproducible.** Resolved by recording the
   complete focused/full PowerShell wrappers, exact hygiene command, and
   SHA-256 plus byte counts for both ignored historical sources.

Scoped Task 1 quality re-review: **PASS**. Task 2's final review corrections
are addressed in this documentation-only round; controller re-review is
pending.

### Specialist review findings and resolutions

Initial paper-safety result: **FAIL**.

1. **HIGH — mutable settings could weaken the required `DU` policy.**
   `REQUIRE_PAPER_ACCOUNT_PREFIX=False` removed account-prefix validation, and
   changing `PAPER_ACCOUNT_PREFIX` made the validator trust the substituted
   prefix. Resolved by rejecting either policy mutation, then unconditionally
   validating the account against literal `DU` before `_load_ib_class` or
   client construction. The two regression tests were observed RED before the
   production correction and GREEN afterward.

Scoped Task 1 paper-safety re-review: **PASS**. Task 2 changes no paper-safety
behavior; final correction-round re-review is pending. The session-wide
`reqGlobalCancel` limitation remains disclosed and must not be reclassified as
account-isolated behavior during review.

## Known limitations

1. The default command is not dry-run. It connects and can transmit paper
   orders; automated tests safely characterize that path with a fake only.
2. The explicit cancellation path uses `reqGlobalCancel`, which is
   session-wide. It is not isolated by account, client ID, Agent 0 order
   reference, or local tracking.
3. `submit_plan` constructs the server-reference map from all visible open
   orders without an account filter; a cross-account order-reference collision
   could suppress an Agent 0 submission.
4. `broker.connect` prints the configured account, and managed-account
   validation errors include the visible account list. Current logging is not
   account-value redacted.
5. Agent 0 performs no position checks or flattening and has no default dry-run
   mode. Those current behaviors are characterized, not approved as the future
   shared paper-agent standard.
6. These are deterministic fake-broker characterization tests, not an IBKR
   connectivity test and not evidence of paper performance.

## Manual gate requested

**MG1 remains pending with conditional user approval.** The user selected and
explained the authoritative 25/week count on 2026-07-29. Effective ledger
approval awaits the fresh Task 2 requirements, quality, and paper-safety
reviews. The final verification evidence in this record passes.

Until those conditions pass:

- MG1 is not recorded as Approved;
- P10 remains unstarted; and
- no broker or external action is authorized.

## Paper-only assertion

The safety correction adds no account, endpoint, production port, bypass,
order path, or cancellation path. It makes the existing `DU` requirement
immutable at validation time and rejects weakened policy settings before IB
class loading/client construction. All execution and cancellation coverage
uses fakes under a hard module-lifetime socket guard. No broker order was
submitted.
